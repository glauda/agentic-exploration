[project]
name = "agentic-exploration"
version = "0.1.0"
description = "Exploration on agentic frameworks"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=1.9.0",
    "litellm>=1.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.7",
]
